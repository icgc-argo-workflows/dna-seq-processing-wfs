includeConfig '../nextflow.config'

params {
    api_token = "$api_token"  // take token from ENV `export api_token=your_token`
}

process {  // default settings
    container = 'ubuntu:18.04'
    cpus = 1
    memory = 1.GB
}
